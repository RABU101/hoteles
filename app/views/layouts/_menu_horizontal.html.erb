<nav class="navbar navbar-light bg-primary fixed-top">
    <div class="container">
        <div class="row w-100 pt-4 pb-4">
            <div class="col-2 text-white mb-1">
                <%= link_to "HCampestre🌴", root_path, class: 'text-decoration-none text-white fs-3'%>
            </div>
            <div class="col text-center">    
                <!-- usar un form con GET  -->
                <%= form_with(url: "/", method: "get", class: 'row') do |formulario| %>          
                    <div class="col-10 text-center mb-1">
                        <%= formulario.text_field :busqueda, class: 'form-control', placeholder: "Buscar: hotel o ciudad" %>
                    </div>
                    <div class="col-2 text-center mb-1"> 
                        <%= formulario.submit "Buscar", class: 'btn btn-secondary text-white' %>
                    </div>                
                <% end %>
            </div>
            <div class="col-3 text-end">
                <% if @usuario_actual %>
                    <div class="nav-item dropdown">                
                        <%= link_to "#{@usuario_actual.nombre}", "#", class: 'btn btn-primary text-white dropdown-toggle', id: 'navbarDropdown', role: 'button', 'data-bs-toggle':'dropdown', 'aria-expanded': false %>
                        <ul class="dropdown-menu" aria-labelledby="NavbarDropdown">
                            <li>
                                <%= link_to "Ver Perfil", usuario_path, class: 'dropdown-item' %>
                            </li>
                            <li><hr class="dropdown-divider"></li>
                            <li>
                                <%= link_to "Cerrar Sesión", cerrar_sesion_path, method: :delete, class: 'dropdown-item' %>
                            </li>
                        </ul>
                    </div>
                <% else  %>
                        <%= link_to "💻 Registrarse", registro_path, class: 'btn btn-primary text-white' %>
                        <% if !@usuario_actual %>                            
                            <%= link_to "💻 Iniciar Sesión", iniciar_sesion_path, class: 'btn btn-primary text-white' %>                            
                        <% end %>
                <% end %>
            </div>       
        </div>        
    </div>
</nav>